.single-event
  .indicator-wrapper
    = render partial: 'modules/categories/indicator', locals: { color: entry.color }
  = link_to event_single_event_path(*entry.path_info), class: "short-description"  do
    %h3= entry.title
    %h4= entry.subtitle
    %p.additional-info= entry.additional_info
