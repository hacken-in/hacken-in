.styleguide-example

  %h3= @section.section

  .styleguide-description.markdown-body
    %p= convert_markdown h(@section.description)
    - if @section.modifiers.any?
      %ul.styleguide-modifier
        - modifiers.each do |modifier|
          %li
            %strong= modifier.name
            \- #{modifier.description}

  .styleguide-element
    = @example_html.gsub('$modifier_class', '').html_safe
  %code.styleguide-source
    = @example_html.gsub('$modifier_class', '')

  .styleguide-html
    = @example_html

  - modifiers.each do |modifier|
    .styleguide-element.styleguide-modifier
      .styleguide-modifier-name= modifier.name
      = @example_html.gsub('$modifier_class', " #{modifier.class_name}").html_safe

</div>
