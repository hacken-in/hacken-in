.calendar-days
  - days.each do |day|
    = render partial: "/modules/calendars/day", locals: {day: day}

:javascript
  var calendarNextDate   = "#{days.last.date + 1.day}";
  var endOfTheWorld      = false;
