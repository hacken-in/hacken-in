<url>
  <loc><%= request.base_url %>/humans</loc>
</url>
<url>
  <loc><%= request.base_url %>/impressum</loc>
</url>
<url>
  <loc><%= request.base_url %>/statistics</loc>
</url>
<% @single_events.each do |single_event| %>
<url>
  <loc><%= event_single_event_url(single_event.event, single_event) %></loc>
  <lastmod><%= single_event.updated_at.utc.iso8601 %></lastmod>
</url>
<% end %>
